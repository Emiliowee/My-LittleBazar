/**
 * Temas extra (Nebula) — RETIRADOS.
 *
 * Los presets "Notion/Duolingo" no se adaptaban bien a toda la app (muchas
 * pantallas tienen color propio), así que se quitaron. La apariencia ahora es
 * simple y bien cuidada: CLARO (estándar) u OSCURO (dark "Linear", no negro
 * total), más "Como Windows". Ver src/index.css y ThemeProvider.
 *
 * Se deja el arreglo vacío para no romper imports existentes.
 */
export const NEBULA_PRESETS = []
